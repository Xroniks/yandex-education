mixin inputArea(type,placeholderText,nameInput,nameInputText)
  label.inputAreaLabel(for=nameInput)= nameInputText
    input.inputAreaWindow(
      type=type,
      name=name,
      id=name,
      placeholder=placeholderText,
      value=""
    )
